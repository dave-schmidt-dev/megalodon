[pytest]
testpaths = ui/tests/unit ui/tests/integration
markers =
    unit: pure-function tests, no server, <1s each
    integration: API x filesystem tests, ~5-10s each
addopts = -ra --strict-markers
