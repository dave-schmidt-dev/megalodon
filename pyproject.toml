[project]
name = "megalodon"
version = "9.4.0"
requires-python = ">=3.11"

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-asyncio",
    "freezegun",
    "pytest-forked",
    "fastapi",
    "uvicorn[standard]",
    "sse-starlette",
    "pyyaml",
    "httpx",
]
