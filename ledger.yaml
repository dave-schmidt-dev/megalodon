# megalodon ledger — minimal machine state. NO `freeze` field (computed on read).
invariants:
  INV-1: {gate_test_status: covered}    # test_csrf_canonical_routes.py landed this session
  INV-2: {gate_test_status: covered}    # test_state_source_of_truth.py — read==write MISSION.md
  INV-3: {gate_test_status: covered}    # chromium-mutations now CI-gated
resolutions:
  INV-1: {resolved_at_date: "2026-05-26", note: "Fix R3 server-enforced gating"}
  INV-2: {resolved_at_date: "2026-05-26", note: "post_v1_mission_status now writes MISSION.md/**Status:** (same file+marker as _read_mission_md_fields); appends Status line if absent; gate test test_state_source_of_truth.py 4/4 green"}
  INV-3: {resolved_at_date: "2026-05-27", note: "CI re-enabled with cost guardrails — ubuntu-only (no macos), concurrency cancel-in-progress, push scoped to main + docs paths-ignore, per-job timeout-minutes, account-level Actions budget cap set by operator. Blocking gate keeps chromium-board/default/mutations; webkit non-blocking. Guardrails regression-locked by scripts/tests/test_ci_workflow_guardrails.py."}
